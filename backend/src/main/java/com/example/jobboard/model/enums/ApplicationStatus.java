package com.example.jobboard.model.enums;

/** Tracks the progression of a job application through the hiring pipeline. */
public enum ApplicationStatus {
    APPLIED,
    SHORTLISTED,
    INTERVIEW,
    ACCEPTED,
    REJECTED
}
