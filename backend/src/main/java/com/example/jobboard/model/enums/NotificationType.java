package com.example.jobboard.model.enums;

/** Categorizes in-app notifications for display and filtering. */
public enum NotificationType {
    APPLICATION_RECEIVED,
    APPLICATION_STATUS_UPDATED,
    EMPLOYER_APPROVED,
    EMPLOYER_REJECTED,
    NEW_JOB_POSTED,
    GENERAL
}
